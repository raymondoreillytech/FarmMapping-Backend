AWSTemplateFormatVersion: "2010-09-09"
Description: ECS service and task definition for FarmMapping backend.

Parameters:
  AppName:
    Type: String
    Default: farmmapping
    Description: Prefix for exported names.
  AppImageTag:
    Type: String
    Default: latest
    Description: ECR image tag for the backend container.
  InferenceImageTag:
    Type: String
    Default: latest
    Description: ECR image tag for the inference sidecar container.
  EcrRepositoryName:
    Type: String
    Default: farmmapping-backend
    Description: ECR repository name for the backend image.
  InferenceEcrRepositoryName:
    Type: String
    Default: farmmapping-inference
    Description: ECR repository name for the inference image.
  ContainerPort:
    Type: Number
    Default: 8080
    Description: Container port exposed by the Spring app.
  InferenceContainerPort:
    Type: Number
    Default: 8000
    Description: Container port exposed by the inference sidecar.
  AppDesiredCount:
    Type: Number
    Default: 0
    Description: Number of running tasks.
  AppCpu:
    Type: String
    Default: "1024"
    Description: Task CPU units for the combined app and inference task.
  AppMemory:
    Type: String
    Default: "2048"
    Description: Task memory (MiB) for the combined app and inference task.
  HealthCheckGracePeriodSeconds:
    Type: Number
    Default: 300
    Description: Grace period for ALB health checks during task startup.
  LogRetentionDays:
    Type: Number
    Default: 1
    Description: CloudWatch log retention in days.

Resources:
  EcrRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref EcrRepositoryName
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Retain only the last 5 images",
                "selection": {
                  "tagStatus": "any",
                  "countType": "imageCountMoreThan",
                  "countNumber": 5
                },
                "action": {
                  "type": "expire"
                }
              }
            ]
          }

  InferenceEcrRepository:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: !Ref InferenceEcrRepositoryName
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          {
            "rules": [
              {
                "rulePriority": 1,
                "description": "Retain only the last 5 images",
                "selection": {
                  "tagStatus": "any",
                  "countType": "imageCountMoreThan",
                  "countNumber": 5
                },
                "action": {
                  "type": "expire"
                }
              }
            ]
          }

  AppLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/ecs/${AppName}"
      RetentionInDays: !Ref LogRetentionDays

  TaskExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Policies:
        - PolicyName: ssm-parameter-read
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - ssm:GetParameters
                Resource:
                  - !Sub
                    - "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter${DbUsernameParam}"
                    - DbUsernameParam: !ImportValue
                        Fn::Sub: "${AppName}-db-username-param"
                  - !Sub
                    - "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter${DbPasswordParam}"
                    - DbPasswordParam: !ImportValue
                        Fn::Sub: "${AppName}-db-password-param"

  AppTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub "${AppName}-task"
      RequiresCompatibilities:
        - EC2
      NetworkMode: awsvpc
      Cpu: !Ref AppCpu
      Memory: !Ref AppMemory
      ExecutionRoleArn: !GetAtt TaskExecutionRole.Arn
      ContainerDefinitions:
        - Name: inference
          Image: !Sub "${InferenceEcrRepository.RepositoryUri}:${InferenceImageTag}"
          Essential: true
          PortMappings:
            - ContainerPort: !Ref InferenceContainerPort
          HealthCheck:
            Command:
              - CMD-SHELL
              - python -c "from urllib.request import urlopen; urlopen('http://127.0.0.1:8000/health').read()" || exit 1
            Interval: 30
            Timeout: 5
            Retries: 3
            StartPeriod: 60
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref AppLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: inference
        - Name: app
          Image: !Sub "${EcrRepository.RepositoryUri}:${AppImageTag}"
          Essential: true
          DependsOn:
            - ContainerName: inference
              Condition: HEALTHY
          PortMappings:
            - ContainerPort: !Ref ContainerPort
          Environment:
            - Name: SPRING_DATASOURCE_URL
              Value: !Sub
                - "jdbc:postgresql://${DbHost}:${DbPort}/${DbName}"
                - DbHost: !ImportValue
                    Fn::Sub: "${AppName}-db-endpoint"
                  DbPort: !ImportValue
                    Fn::Sub: "${AppName}-db-port"
                  DbName: !ImportValue
                    Fn::Sub: "${AppName}-db-name"
            - Name: TILES_BASE_URL
              Value: /tiles
            - Name: TREE_INFERENCE_BASE_URL
              Value: !Sub "http://127.0.0.1:${InferenceContainerPort}"
          Secrets:
            - Name: SPRING_DATASOURCE_USERNAME
              ValueFrom: !ImportValue
                Fn::Sub: "${AppName}-db-username-param"
            - Name: SPRING_DATASOURCE_PASSWORD
              ValueFrom: !ImportValue
                Fn::Sub: "${AppName}-db-password-param"
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref AppLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: ecs

  AppService:
    Type: AWS::ECS::Service
    Properties:
      Cluster: !ImportValue
        Fn::Sub: "${AppName}-ecs-cluster-name"
      DesiredCount: !Ref AppDesiredCount
      LaunchType: EC2
      TaskDefinition: !Ref AppTaskDefinition
      AvailabilityZoneRebalancing: DISABLED
      DeploymentConfiguration:
        MaximumPercent: 100
        MinimumHealthyPercent: 0
      HealthCheckGracePeriodSeconds: !Ref HealthCheckGracePeriodSeconds
      NetworkConfiguration:
        AwsvpcConfiguration:
          Subnets: !Split
            - ","
            - !ImportValue
              Fn::Sub: "${AppName}-public-subnet-ids"
          SecurityGroups:
            - !ImportValue
              Fn::Sub: "${AppName}-task-sg-id"
      LoadBalancers:
        - ContainerName: app
          ContainerPort: !Ref ContainerPort
          TargetGroupArn: !ImportValue
            Fn::Sub: "${AppName}-tg-arn"

Outputs:
  EcrRepositoryUri:
    Value: !GetAtt EcrRepository.RepositoryUri
    Export:
      Name: !Sub "${AppName}-ecr-repo-uri"
  InferenceEcrRepositoryUri:
    Value: !GetAtt InferenceEcrRepository.RepositoryUri
    Export:
      Name: !Sub "${AppName}-inference-ecr-repo-uri"
  EcsServiceName:
    Value: !Ref AppService
    Export:
      Name: !Sub "${AppName}-ecs-service-name"
